RewriteEngine On

# If requested file or folder exists, serve it normally
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise, redirect everything to index.html
RewriteRule ^ index.html [L]
